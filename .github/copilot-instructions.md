# Copilot Instructions

Use `AGENTS.md` as the canonical repository guidance.
Follow `docs/workflow.md` for workflow, labels, branches, commits, and delivery.
Use commands from the root `package.json`.

Keep changes small, prefer the nearest existing test, and avoid duplicating rules
that already live in `AGENTS.md` or `docs/`.
