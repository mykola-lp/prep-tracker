# Production environment variables
NGINX_PORT=8081
API_PORT=3001

NGINX_PORT=8081

API_PORT=3001
WEB_PORT=5173

VITE_API_BASE_URL=/api

JWT_SECRET=super_secret_key_123

NODE_ENV=production

# Production PostgreSQL container (Uncomment these values for local database)
DB_HOST=postgres
DB_PORT=5432
DB_USER=prep_tracker
DB_PASSWORD=prep_tracker
DB_NAME=prep_tracker

# Aiven PostgreSQL (Uncomment these values instead of the local database values)
# only from separate connection
